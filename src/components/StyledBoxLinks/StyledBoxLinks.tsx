import styled from "@emotion/styled";

import { Box } from "@mui/material";

const StyledBoxLinks = styled(Box)({
 display: "flex",
 justifyContent: "space-between",
});

export default StyledBoxLinks;
